import SwiftUI

struct MyPageView: View {
    var body: some View {
        NavigationStack {
            Text("Search")
                .font(.title)
                .navigationTitle("検索")
        }
    }
}
