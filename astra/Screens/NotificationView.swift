import SwiftUI

struct NotificationView: View {
    var body: some View {
        NavigationStack {
            Text("Profile")
                .font(.title)
                .navigationTitle("プロフィール")
        }
    }
}
